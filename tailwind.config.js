/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    // Add paths to all your template files (e.g., Go/Templ files, HTML, JS)
    "./**/*.html",
    "./**/*.templ",
    "./**/*.go",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
